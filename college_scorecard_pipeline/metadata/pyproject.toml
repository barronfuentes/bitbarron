[project]
name = "column_sync"
version = "0.1.0"
description = "a CLI tool that reads the data dictionary from `college_scorecard_pipeline/metadata/data.yaml` and programmatically updates column descriptions (comments) in Databricks Unity Catalog via the Databricks REST API."
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    # Runtime dependencies go here
]

authors = [{ name = "barronfuentes@gmail.com" }]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
]

[project.scripts]
column-sync = "column_sync.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/column_sync"]

[tool.ruff]
line-length = 120
